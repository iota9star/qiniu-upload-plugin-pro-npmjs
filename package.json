{
  "name": "qiniu-upload-plugin-pro",
  "version": "1.0.2",
  "description": "将webpack项目打包到七牛",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "./node_modules/.bin/rimraf lib",
    "build": "./node_modules/.bin/babel src --out-dir lib",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "qiniu",
    "qiniu upload",
    "vue pack",
    "webpack",
    "upload",
    "cdn"
  ],
  "author": "iota9star",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iota9star/qiniu-upload-plugin-pro-npmjs.git"
  },
  "dependencies": {
    "ora": "^3.0.0",
    "qiniu": "^7.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
